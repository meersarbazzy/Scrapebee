services:
  scrapebee:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./universal_output_final:/app/universal_output_final
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
